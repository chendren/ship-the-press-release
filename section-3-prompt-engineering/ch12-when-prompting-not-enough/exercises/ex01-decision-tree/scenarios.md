# Scenarios: What's the Right Tool?

1. You need the model to always format dates as YYYY-MM-DD
2. You need the model to check if a Docker container is running before deploying
3. You need the model to follow your team's Git commit message convention
4. You need the model to never expose API keys in responses
5. You need the model to understand your company's domain-specific terminology
