# Exercise 10.1: The Trap in Action

## Goal
Diagnose whether a prompt problem is actually a prompt problem — or something else entirely.

## Instructions

1. Read `bad-prompts.md` — five prompts that produce inconsistent results
2. For each: diagnose the real problem (prompt? context? tool choice? spec?)
3. Propose a fix that ISN'T "make the prompt longer"
